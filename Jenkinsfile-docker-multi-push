pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t priyankaguptap/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag priyankaguptap/request-logger:${env.BUILD_ID} priyankaguptap/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push priyankaguptap/request-logger:${env.BUILD_ID}"
        sh "docker push priyankaguptap/request-logger:latest"
      }
    }
  }
}
