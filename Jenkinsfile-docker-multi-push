pipeline {
  agent any

  stages {
    
    stage('Build Image') {
      steps {
      sh "docker build -t PriyankaPantangi/request-logger:${env.BUILD_ID} -f Dockerfile-multi ."
      sh "docker tag PriyankaPantangi/request-logger:${env.BUILD_ID} PriyankaPantangi/request-logger:latest"
      }
    }
    
  }

  post {
    success {
      withCredentials([usernamePassword(credentialsId: 'docker-hub-credentials', usernameVariable: 'USERNAME', passwordVariable: 'PASSWORD')]) {
        sh "docker login -u ${USERNAME} -p ${PASSWORD}"
        sh "docker push PriyankaPantangi/request-logger:${env.BUILD_ID}"
        sh "docker push PriyankaPantangi/request-logger:latest"
      }
    }
  }
}
